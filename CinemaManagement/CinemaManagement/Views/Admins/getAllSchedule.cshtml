@model IEnumerable<CinemaManagement.Models.Schedule>

@{
    ViewBag.Title = "getAllSchedule";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>getAllSchedule</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.id_Sche)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_Ses)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sche_Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_F)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_Sche)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_Ses)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sche_Date)
            </td>

            <td>
                @Html.DisplayFor(modelItem => item.id_F)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>
